
import {
    Flex,
} from '@chakra-ui/react'


export const MoreMenuWrapperTopPart = ({children}) => {

    return (
        <Flex
            position='relative'
            
            >
            {children}
        </Flex>
    )
}